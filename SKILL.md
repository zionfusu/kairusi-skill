---
name: kairusi
description: 通过 Kairusi MCP 统一管理日刻日记、Todo 文件夹、清单和任务，读取已有标签为任务做场景化分类，以及管理日历日程。用户要记录或查看日记、整理或处理待办、查询或安排日程，或要求在这三个产品之间协同时使用。
metadata:
  version: "1.2.3"
---

# Kairusi

使用已连接的 Kairusi MCP 完成日记、任务和日程工作。Kairusi 将日刻、Todo 和日历收敛到一个连接中；工具按 `rike_`、`todo_`、`calendar_` 命名，必须先根据用户的真实目标选择正确命名空间。

每次建立新的 MCP 连接后，先调用 `kairusi_get_version` 并传入 `skill_version: "1.2.3"`。若返回 `update_required: true`，告知用户更新 Skill；在版本满足最低要求前不要执行写入操作。不要把 MCP 服务版本误认为本地 Skill 版本。

## 连接与授权

首次使用时，提示用户打开 `https://mcp.kairusi.com/authorize`，选择需要的产品并完成登录。用户随后将授权页生成的 MCP 配置添加到 AI 客户端。不要索取、打印、保存或复述用户密码、产品会话 ID 或 Kairusi Bearer Token。

统一 Kairusi Token 默认有效期为 30 天，并采用不可解码的随机格式。任一产品的上游登录会话也可能独立提前失效；如果工具提示授权缺失、会话失效或登录失效，要求用户重新访问授权页并替换其 MCP 配置。重新授权会主动撤销该账号此前的 Kairusi Token。若用户表示 Token 或 MCP 配置泄露，立即指导其重新授权，或使用授权结果页的“立即撤销当前授权”；不要要求用户把泄露的 Token 发到对话中。不要自动重试写入操作，也不要把任何凭证放入对话、日志或文件。

## 随包命令行客户端

当当前 AI 应用不能直接添加 MCP，或需要从终端调用服务时，优先使用 `scripts/kairusi_mcp.py`，不要绕过统一服务直接调用日刻、Todo 或日历内部 API。脚本需要 Python 3，不需要安装第三方依赖。

1. 要求用户先在授权页生成 MCP 配置，再在本地终端运行 `python3 scripts/kairusi_mcp.py configure`。Token 输入不会回显，并仅保存在 `~/.config/kairusi-skill/token`；脚本不会打印 Token。
2. 调用 `python3 scripts/kairusi_mcp.py list-tools` 验证授权，并按需要使用 `call --tool <工具名> --arguments '<JSON对象>'`。
3. 具体命令示例读取 `references/api-commands.md`。不要在命令历史、配置仓库或对话中粘贴 Token。

## 选择正确产品

| 用户目标 | 产品与工具前缀 | 推荐顺序 |
|---|---|---|
| 写、查看或整理九宫格日记 | 日刻，`rike_` | 先读取日记本、模板和标签，再创建或修改日记。 |
| 创建文件夹、清单，或搜索、完成、修改待办 | Todo，`todo_` | 先读取现有结构和标签，再创建容器或定位任务。 |
| 查看日程、安排会议或设置提醒 | 日历，`calendar_` | 先确认日历和写入权限，再读取或创建日程。 |
| 把任务安排到日历、根据日程写日记 | 跨产品协作 | 先读取源对象，向用户说明将被写入目标产品的内容，再执行。 |

当用户的表述含糊时，先询问是要处理“日记、任务还是日程”。不要把建议、草稿或待讨论事项自动写入任何产品。

## 日刻工作流

创建或更新日记前，调用日刻资源读取工具获取准确的日记本 UUID、模板 UUID、模板格子标题和可选标签 UUID。为每个格子传入 `title`、`content` 与正确的 `order_by`，其中标题优先采用所选模板在 `template_cells` 中相同顺序的真实标题；不能取得时可省略，由 MCP 使用“记录 N”作为非空标题。MCP 会按照日刻接口约定补齐日记 UUID、格子 UUID、纯文本、H5 内容和内容长度。新增或更新后必须通过详情接口回读同一日记 UUID，并逐格确认内容与提交值相同；回读不到或内容不一致时不得报告成功。更新已有日记时，先读取完整详情并保留每个格子的 `note_child_uuid`，避免覆盖或重复创建未提及的格子。

删除日记前，先读取并向用户复述目标日记的日期与日记本。只有在用户对该具体日记作出明确确认后，才调用删除工具并传入 `confirmed: true`。

## Todo 工作流

创建文件夹使用 `todo_create_folder`。创建清单使用 `todo_create_list`；先调用 `todo_list_projects` 查看现有结构，确认是否需要放入某个文件夹。清单必须提供运行时 schema 要求的 `image_url` 图标 ID；若用户希望沿用现有清单图标，可从读取结果中取得，无法确定时应询问用户，不要编造图标 ID。需要归入文件夹时，把准确的文件夹 UUID 作为 `folder_uuid` 传入。每个账号都固定存在唯一的系统“收件箱”；绝不创建名为“收件箱”的清单，也不创建任何替代收件箱。需要其 UUID 时调用 `todo_get_default_lists` 并使用 `inbox.list_uuid`。

创建任务前调用 `todo_list_labels` 读取用户真实存在的标签。用户明确指定清单时，先读取项目和清单并使用准确的清单 UUID；用户没有指定清单时，不要追问，也不要创建清单：可先调用 `todo_get_default_lists` 确认系统“收件箱”，创建任务时省略 `list_uuid`，由 MCP 再从专用默认清单接口读取 `data.sjx` 并写入真实收件箱。根据任务标题、备注、地点、所属清单和时间语义选择最贴近场景的少量标签，例如工作事务匹配事业或具体项目标签、运动就医匹配健康、学习训练匹配成长、购物理财匹配财富、聚会联络匹配朋友或家庭、娱乐出游匹配休闲。以用户已有标签的实际名称和层级为准，不依赖固定标签名称。

标签匹配应克制：只选择语义证据明确的标签，优先具体的二级标签；只有父子标签都能为用户提供检索价值时才同时绑定。标签是只读词表：不得编造 UUID，不得创建、修改或删除标签。没有匹配项时不加标签。若任务跨多个明显场景，可绑定多个已有标签；语义模糊时不加标签，或在标签会显著影响整理结果时询问用户。

创建任务时把匹配结果放入 `label_uuids`。更新任务标签前先读取现有任务；`label_uuids` 是完整替换列表，应保留仍然适用且用户没有要求移除的原标签。修改、完成或删除任务前，先搜索或列出任务，确认唯一的任务、所属清单和当前状态。任务完成使用状态切换工具，而不是删除。

删除任务前，展示任务标题和所属清单，并取得明确确认。对于“清空”“删除旧任务”等模糊请求，先展示候选对象并询问范围，绝不批量删除。

## 日历工作流

写入前先读取日历，确认目标日历 `can_write` 为 true。读取时间范围日程时，使用 UTC ISO 8601 的开始和结束时间，并传入用户的 IANA 时区；时区不明确时先询问。

创建日程前明确标题、目标日历、开始时间、结束时间或时长。日程写入由 MCP 按官方契约统一调用 `event/commit`；新增后必须通过 `event/get_events` 回查相同 UUID，只有确认日程真实存在才可向用户报告成功，不能仅根据提交接口返回 `res=200` 宣称成功。更新日程前先读取原日程，以保留未被用户要求修改的核心字段。删除前复述标题和开始时间，并获得针对该日程的明确确认后才调用删除工具。

## 跨产品协作与隐私

仅在用户明确要求时跨产品复制信息。例如，用户可要求“把这个任务排进日历”或“按今天日程写一篇日记”。先读取源对象，再说明将写入目标产品的摘要；默认不要复制私密日记正文、备注或附件。

需要核对完整工具、字段和权限语义时，读取 `references/product-mapping.md`；需要执行脚本命令示例时，读取 `references/api-commands.md`。
